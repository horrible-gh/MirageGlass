---
next_type: T
next_type_detail: 작업지시
project: mirageglass
module: default
group: 0005
type: T
type_detail: 작업지시
title: 덱 다운로드/매니페스트 엔드포인트 구현
target_id: R0001
---

# 1. 목적 / 배경

R0001("AI가 이것저것 수정하려면 다운로드가 가능해야 할 것 같다")에 대해 NR0003이 조사한 결과,
등록된 덱의 소스를 회수할 수단이 현재 전무함이 확인되었다. 덱 수정 왕복 3단계 중 (1)만 비어 있다.

```
(1) 현재 덱의 소스를 받아온다      <- 수단 없음   ★본 작업으로 채운다
(2) 로컬에서 수정한다
(3) DELETE 후 POST 로 재등록한다   <- 이미 가능
```

본 T는 NR0003의 권장안(신규 엔드포인트 2개)을 그대로 구현하도록 지시한다.
이 작업의 핵심 가치는 **왕복 대칭성** — 다운로드한 zip을 아무 가공 없이 그대로
`POST /api/v1/decks` 에 다시 올리면 성공해야 한다.

# 2. 범위

## 포함

- `GET /api/v1/decks/{deck_id}/download` — `storage/decks/{id}/src` 전체를 zip으로 묶어 반환
- `GET /api/v1/decks/{deck_id}/files` — src 기준 상대 경로 파일 매니페스트 (NR D-3: 이번 T에 포함)
- 위 2건에 대한 help 문서 갱신 및 개수 하드코딩 테스트 수정
- 테스트 추가, README / 설계 draft 갱신

## 제외 (건드리지 말 것)

- 업로드 원본 zip의 영구 보존 (`create_deck()` 의 `zip_path.unlink()` 흐름 변경 금지)
- 덮어쓰기(PUT) 지원
- 재등록 시 안정 링크(`group_key` 활용) — NR D-2. 별도 R 요건으로 분리한다
- DB 스키마 / 마이그레이션 / `server/sql/queries/queries.json` — **변경 불필요**
- `to_public()` 응답 스키마 확장 — 목록/단건/생성이 공유하므로 흔들지 않는다

## 전제 결정사항

- **인증: 무인증**으로 구현한다. 기존 규칙("읽기 동작은 인증 불필요", `require_token` 은
  `POST /api/v1/decks` 와 `DELETE /api/v1/decks/{id}` 에만 적용)과 정합적이며,
  `/v/{id}/{path}` 로 이미 모든 자산이 무인증 공개되어 있다 (NR D-1 권장안).
  ※ 본 건에 대해 R0001에 질의(Q)를 등록해 두었다. **유인증으로 답변이 오면** TR 착수 전에
  `require_token` 의존성 추가 + help 의 `auth.required_for` 갱신 +
  `tests/test_help_endpoint.py::test_auth_flags_match_the_auth_block` 동반 수정으로 전환할 것.
  답변이 없으면 위 기본값(무인증)대로 진행한다.

# 3. 작업 항목 (순서대로)

## W-1. `server/storage.py` — 파일 열거 / 아카이브 생성 [P1]

기존 레이어 분리("경로 해석·파일 조작은 `storage.py`, 라우팅은 `api.py`")를 유지한다.
`deck_src_dir()` / `resolve_asset()` 바로 아래에 다음 2개를 추가한다.

- `iter_deck_files(settings, deck_id) -> list[tuple[str, int]]`
  - `deck_src_dir(settings, deck_id).resolve()` 를 루트로 `rglob("*")` 순회
  - 파일만 포함(디렉터리 제외), 상대 경로 구분자는 **항상 `/`** (`PurePath.as_posix()`)
  - **경로 탈출 재확인**: 각 항목의 `resolve()` 결과가 루트 하위인지 검사하고 아니면 건너뛴다.
    업로드 시 `inspect_zip` 이 심볼릭 링크를 거부하지만 다운로드 코드가 그 가정에 기대지 않게 한다
  - 경로 **사전순 정렬 고정**(재현성). src 디렉터리가 없으면 `None` 반환 등 "없음"을 구분 가능하게 할 것
- `build_deck_archive(settings, deck_id, out_path) -> bool`
  - `iter_deck_files()` 가 낸 목록만 `zipfile.ZipFile(..., ZIP_DEFLATED)` 로 기록
  - **zip 루트 = `src` 루트**. 즉 `index.html` 이 zip 최상위에 위치해야 한다(재등록 시 400 회피 조건)
  - 최대 500MB / 5000엔트리까지 커질 수 있으므로 **메모리에 zip을 적재하지 말 것**.
    파일 단위 스트리밍으로 `out_path` 에 직접 쓴다

## W-2. `server/api.py` — 라우트 2개 추가 [P1]

기존 라우트와 동일하게 **동기 `def`** 로 선언한다(zip 생성은 블로킹 I/O → FastAPI 스레드풀).

- `GET /api/v1/decks/{deck_id}/download` → `application/zip`
  - 상태별 응답: 덱 행 없음 → **404**, `status != "ready"` → **409**, `src` 디렉터리 없음 → **404**
  - 임시 zip은 반드시 **`settings.tmp_dir`** 아래에 만든다(시스템 temp 사용 금지 —
    `MIRAGEGLASS_STORAGE` 로 저장 위치를 격리하는 테스트 전제가 깨진다)
  - `FileResponse` + `starlette.background.BackgroundTask(unlink)` 로 응답 후 임시 파일 정리
  - `Content-Disposition` 파일명은 **`{deck_id}.zip` 으로 ASCII 고정**.
    덱 `name` 은 비ASCII 허용이므로(`"Lifecycle deck — café デッキ"` 왕복 테스트 존재)
    헤더에 name 을 넣지 말 것
  - 동시성: 생성 중 `DELETE` 와 경합해 파일이 사라지면 500이 아니라 **404/409로 매핑**한다
- `GET /api/v1/decks/{deck_id}/files`
  - 응답: `{"items": [{"path": "index.html", "size": 1234}, {"path": "assets/style.css", "size": 14}]}`
  - `path` 는 src 기준 상대 경로, 구분자 `/`, 디렉터리 제외, 사전순 정렬
  - 상태별 응답은 download 와 동일 규칙(404 / 409)

## W-3. `server/api.py` — help 문서 갱신 [P2]

- `help_document()` 의 `endpoints` 배열에 위 2건 추가. 각 항목의 `auth` 플래그는 `false`(무인증)
- `workflow` 에 "덱 수정 왕복(download → 편집 → DELETE → POST)" 절차를 반영
- `gotchas` 에 다음을 반영: 다운로드 zip은 업로드 원본의 바이트 사본이 아니라 `src` 재압축본이라는 점,
  재등록하면 `deck_id` / `viewer_url` 이 바뀐다는 점

## W-4. 테스트 [P1]

- `tests/test_help_endpoint.py`
  - **`test_help_top_level_shape` 의 `len(endpoints) == 8` 하드코딩을 10으로 수정 — 미수정 시 반드시 실패한다**
  - W-3에서 `workflow` / `gotchas` 문구를 늘렸다면 `len(workflow) == 4`, `len(gotchas) == 5` 도 함께 조정
  - `test_auth_flags_match_the_auth_block` 이 그대로 통과하는지 확인(무인증이면 수정 불필요)
- `tests/test_deck_api.py` — 최소 아래 케이스를 추가한다
  1. **왕복(가장 중요)**: 덱 등록 → `/download` 로 받은 zip 바이트를 **가공 없이 그대로**
     `POST /api/v1/decks` 에 재등록 → 201/성공. 새 덱의 `/v/{new_id}/index.html` 이 원본과 동일 내용
  2. 존재하지 않는 deck_id → 404 (download / files 양쪽)
  3. `status != "ready"` 인 덱 → 409
  4. 매니페스트 경로 집합이 업로드한 zip의 파일 집합과 일치, 정렬이 사전순, 구분자가 `/`
  5. 중첩 디렉터리 자산(`assets/...`)이 zip 안에서 동일한 상대 경로로 재현되는지
- `tests/test_source_is_english.py` 는 추적 텍스트 파일의 한글을 실패로 처리한다.
  **신규 코드·주석·docstring·help 문자열·README 추가분 전부 영어로 작성할 것.**

## W-5. 문서 [P2]

- `README.md` — "덱 수정하기(다운로드 → 편집 → 재등록)" 절 추가
- `docs/mirageglass-api-db-storage-draft.md` — 엔드포인트 표에 2행 추가

# 4. 완료 기준 (TR이 증명해야 할 것)

TR 문서에는 아래를 **실제 실행 출력과 함께** 제시한다.

1. **전체 테스트 GREEN** — `pytest` 전량 통과. 출력 요약(통과/실패 수)을 TR에 붙일 것.
   특히 `tests/test_help_endpoint.py`, `tests/test_deck_api.py`, `tests/test_source_is_english.py`
   3건은 개별 결과를 명시한다
2. **왕복 테스트 통과** — "다운로드한 zip을 그대로 재등록하면 성공한다"가 GREEN.
   이것이 zip 루트 구조 · `index.html` 위치 · 경로 구분자 문제를 한 번에 걸러내는 회귀 안전망이다
3. **에러 매핑 확인** — 미존재 404, 비-ready 409 가 download / files 양쪽에서 테스트로 커버됨
4. **help 정합성** — `GET /api/v1/help` 의 `endpoints` 가 10건이고 신규 2건의 `auth` 플래그가
   실제 라우트 인증 여부와 일치(= `test_auth_flags_match_the_auth_block` 통과)
5. **비적용 확인** — DB 스키마 / `queries.json` / `create_deck()` 의 zip 삭제 흐름 /
   `to_public()` 스키마가 **변경되지 않았음**을 diff로 확인해 명시
6. **변경 파일 요약** — 원격 소스에 write 한 파일 목록과 각 파일의 변경 요지를 TR에 정리

# 5. 참고

- 근거 조사: `mirageglass.default.0005.0003-NR` (다운로드 엔드포인트 조사 결과)
- 원 요건: `mirageglass.default.0005.0001-R` (다운로드 엔드포인트)
- 미결 질의: R0001의 질의 패널 — "다운로드/매니페스트 엔드포인트의 인증 정책 확정 (NR D-1)"
